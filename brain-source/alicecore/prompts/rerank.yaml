# Rerank prompt template (Qwen3-Reranker family).
# Applies only to Qwen3 rerank models (e.g. Qwen3-Reranker-8B / -0.6B).
# Injected when LOCAL mode runs a self-hosted Qwen3-Reranker.
#
# NOTE: `suffix` below carries an upstream artifact — the stray token " spezielle"
# where Qwen3's own chat template expects "<think>\n\n</think>\n\n". Left as-is
# because changing it silently would alter rerank scores; see docs/ENGINE-NOTES.md.

qwen3_rerank:
  description: "Assembles query + documents into the input format the Qwen3-Reranker expects."
  variables:
    - instruction
    - query
    - doc
  prefix: "<|im_start|>system\nJudge whether the Document meets the requirements based on the Query and the Instruct provided. Note that the answer can only be \"yes\" or \"no\".<|im_end|>\n<|im_start|>user\n"
  suffix: "<|im_end|>\n<|im_start|>assistant\n spezielle\n\n"
  query_template: "{prefix}<Instruct>: {instruction}\n<Query>: {query}\n"
  doc_template: "<Document>: {doc}{suffix}"
  default_instruction: "Given a web search query, retrieve relevant passages that answer the query"
